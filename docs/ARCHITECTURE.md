# Архитектура KROT zapret

## Границы процессов

`KROT.exe` работает с обычными пользовательскими правами. Он отвечает за интерфейс, локальные настройки, журнал и команды управления.

`KROT.Service.exe` работает как Windows Service от `LocalSystem`. Только служба может запускать и останавливать runtime-компоненты. GUI связывается со службой через версионированный Named Pipe, отдельный для SID активного пользователя. ACL разрешает доступ этому пользователю, локальным администраторам и `LocalSystem`.

Служба учитывает PID, время запуска, SHA-256 и маркер владения. Глобальное завершение процессов по имени запрещено.

## Runtime

Runtime разделён на две роли:

- `main` — обычные TCP/QUIC-каналы выбранных сервисов;
- `voice` — Discord Voice, STUN и относящийся к голосовой сессии UDP-трафик.

Перезапуск `voice` не затрагивает `main`. Исторические версии движка не смешиваются: каждый комплект находится в отдельной папке со своими DLL, драйвером, Lua и blobs.

В `v0.1.0-alpha` используется `FakeZapretProcessManager`. Реальный процессный менеджер подключается через `IZapretProcessManager` после подготовки и проверки пресетов.

## Состояния

Центральная state machine использует состояния:

```text
Off → Starting → TestingDirect → TestingSavedProfiles → Running → Stopping → Off
```

При необходимости между проверкой сохранённых профилей и запуском появляется `SearchingProfiles`. Ошибки представлены состояниями `PartialFailure` и `FatalError`.

UI вычисляется из состояния и не хранит независимый набор конфликтующих флагов.

## Данные

- `%LocalAppData%\KROT zapret\settings.json` — пользовательские настройки;
- `%LocalAppData%\KROT zapret\logs\` — технические логи;
- `%ProgramData%\KROT zapret\` — профили, списки, кэш и состояние службы;
- `Program Files\KROT zapret\` — только программные файлы.

Настройки сохраняются через временный файл, flush на диск и атомарную замену с резервной копией.

## Следующий этап

1. Пассивная и активная диагностика каналов без авторизации.
2. Контролируемая библиотека быстрых и расширенных пресетов.
3. Проверка целостности runtime перед каждым запуском.
4. Реальный менеджер процессов с Job Object и проверкой владения.
5. Раздельная сборка фильтров `main` и `voice` без пересечений.
6. Проверка на чистых Windows 10/11 x64.

