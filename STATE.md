# KROT zapret — состояние проекта

Дата актуализации: 31 июля 2026
Текущая версия приложения: `1.0`
Основная ветка: `main`
Удалённый репозиторий: `https://github.com/morda-mir/KROT-zapret.git`

Этот файл — техническая точка продолжения разработки. Пользовательское
описание находится в `README.md`.

## 1. Цель продукта

KROT zapret — простая Windows-утилита для доступа к двум сервисам:

- Discord;
- YouTube.

Пользователь не выбирает стратегии, порты, фильтры и версии движка. Он отмечает
нужные сервисы и нажимает одну основную кнопку. Вся техническая конкретика
остаётся в ограниченных по размеру локальных журналах.

KROT не является VPN, не поднимает собственные серверы и не меняет системный
прокси. Обход выполняется локальными процессами Zapret только для выбранных
сервисов.

## 2. Версии и публикация

- Текущая версия проекта и установщика: `1.0`.
- Допустимы только числовые номера из двух–четырёх компонентов: `1.0`, `1.1`,
  `1.0.1`, `2.0`.
- GitHub pre-release, draft-релизы и теги с текстовыми суффиксами приложение
  игнорирует.
- Workflow `build` запускается для `main`, pull request в `main` и числовых
  тегов.
- Workflow `release` запускается вручную, собирает и тестирует установщик,
  публикует временный CI-артефакт и создаёт build provenance attestation.
- Стабильный GitHub Release `1.0` опубликован:
  `https://github.com/morda-mir/KROT-zapret/releases/tag/1.0`.
- Текущий commit и tag `1.0`:
  `63411dbe32b820e260efcde5770dd760e097a9f9`.
- SHA-256 текущего установщика:
  `094de1c7b301121d72d82c19b7853c94c59f550c38f582b380278d96de0dc204`.

## 3. Пользовательский интерфейс

Главное окно:

- WPF, `.NET Framework 4.8`, x64;
- размер `400 × 320`, без изменения размера;
- без стандартной рамки, с собственными кнопками информации, сворачивания и
  закрытия;
- две строки сервисов: Discord и YouTube;
- одна основная кнопка: «Включить», «Отменить» или «Отключить»;
- автозапуск расположен в одной строке с собственной галочкой;
- окно сворачивается в системный трей;
- обычное закрытие выполняет остановку runtime и службы, затем завершает GUI.

Окно «О приложении»:

- помещается без прокрутки;
- показывает версию из метаданных сборки, сейчас `1.0`;
- содержит RU/EN, галочку подробных логов, просмотр журнала;
- содержит ссылки `KROT zapret` и `Zapret`;
- показывает лицензию `KROT zapret · MIT`.

### Индикаторы

Состояния каналов:

- `Disabled` / `Unknown` — серый;
- `WaitingForActivity` — статический жёлтый;
- `Testing` / `Searching` — пульсирующий жёлтый;
- `WorkingDirect` / `WorkingPreset` — зелёный;
- `Failed` — красный.

Discord:

- `text`;
- `media`;
- `voice`.

YouTube:

- `site`;
- `video`.

Для Discord Voice:

- до появления UDP-трафика показывается «Зайдите в голосовой канал»;
- после маркера начала потока — «Проверяю подключение» и пульсация;
- подтверждённый входящий ответ переводит канал в зелёный;
- отсутствие подтверждения после перебора переводит канал в красный;
- новый голосовой поток запускает проверку заново.

### Маскот

Крот построен в `KrotMascot.xaml` отдельными слоями:

- силуэт;
- шахтёрская каска;
- фонарь с отдельными линзой, свечением и лучом;
- неподвижные открытые глаза;
- отдельные закрытые веки для моргания;
- нос;
- усы;
- лапы.

Пассивная анимация срабатывает примерно каждые 3–5 секунд. Во время проверки
хотя бы одного канала крот остаётся неподвижным, фонарь плавно сканирует
пространство из стороны в сторону, а линза и луч мягко пульсируют.

### VPN и прокси

Служба информационно отмечает маршрутизируемый VPN/TUN-адаптер или явно
настроенный системный прокси Windows сообщением «Работа через VPN/прокси».
Этот статус не блокирует проверку и подбор стратегий. HAPP и похожие клиенты
могут оставлять адаптер и маршрут поднятыми при отключённом соединении в своём
интерфейсе, поэтому сообщение описывает фактический маршрут Windows.

При обнаружении рядом с кротом появляется облачко
«Отключи VPN/прокси». После исчезновения внешнего туннеля выполняется
внеплановая проверка каналов. Технические проверки не требуют отдельного
элемента в основном интерфейсе.

## 4. Архитектура процессов

### GUI

`KROT.exe`:

- работает без повышения прав;
- хранит пользовательские настройки и GUI-журнал;
- управляет треем, локализацией и отображением состояний;
- отправляет команды службе через Named Pipe;
- опрашивает состояние службы примерно раз в три секунды;
- при выходе отправляет команду `shutdown`.

### Windows-служба

`KROT.Service.exe`:

- установлена как `KROTZapret`;
- работает от `LocalSystem`;
- имеет тип запуска `Manual`;
- запускается GUI по требованию;
- принимает команды только через версионированный Named Pipe;
- останавливает runtime и себя после команды GUI;
- имеет watchdog: при потере heartbeat GUI runtime и служба завершаются.

IPC protocol version: `5`.

Имя pipe включает SID интерактивного пользователя. ACL разрешает доступ:

- этому пользователю;
- локальным администраторам;
- `LocalSystem`.

Входящие и исходящие JSON-строки имеют ограничения размера, глубины и времени
чтения. Неизвестные команды и несовпадающая версия протокола отклоняются.

### Runtime

Две независимые роли:

- `main` — TCP/TLS для Discord и YouTube на Zapret 1;
- `voice` — UDP/QUIC/STUN/Discord Voice на Zapret 2.

Перезапуск `voice` не должен затрагивать `main`.

`RealZapretProcessManager`:

- проверяет manifest;
- проверяет SHA-256 каждого обязательного файла;
- запускает только разрешённые executable и аргументы;
- хранит PID, время запуска, SHA-256 и ownership marker;
- помещает процессы в Windows Job Object с `KILL_ON_JOB_CLOSE`;
- не завершает процессы глобально по имени;
- выгружает WinDivert только при совпадении пути драйвера с проверенным runtime
  KROT.

Runtime в Debug:

- Zapret 1 `v72.13`;
- Zapret 2 `v1.0.3`;
- резервный комплект Zapret 2 `v1.0.2` пока присутствует, но автоматический
  fallback между версиями ещё не реализован.

## 5. Подбор стратегий

Общий порядок:

1. Проверить сохранённый профиль текущей сети.
2. При его отсутствии или отказе проверить прямой доступ.
3. Если прямой доступ не работает, перебирать каталог независимо от наличия
   внешнего TUN-адаптера или системного прокси.
4. Сохранить победителей по отпечатку сети.
5. Во время работы выполнять лёгкий health monitor.

В текущем каталоге:

- 19 TCP/TLS-вариантов;
- 8 YouTube QUIC-вариантов;
- 10 Discord Voice-вариантов.

Отпечаток сети строится без хранения чувствительных исходных данных. В кэше
сохраняются только SHA-256 отпечатка и идентификаторы выбранных стратегий.

Смена адаптера, Wi-Fi, VPN или прокси будит сетевой монитор. После короткой
стабилизации выполняется внеплановая проверка. Полный обрыв интернета не должен
ошибочно запускать бесконечный подбор.

## 6. Настройки и журналы

Пользовательские данные:

- `%LocalAppData%\KROT zapret\settings.json`;
- `%LocalAppData%\KROT zapret\logs\`.

Служебные данные:

- `%ProgramData%\KROT zapret\service-state\preset-cache.json`;
- `%ProgramData%\KROT zapret\service-state\logs\`.

Настройки:

- сохраняются через временный файл;
- сбрасываются на диск;
- заменяются атомарно;
- имеют резервную `.bak`;
- ограничены размером 1 MiB;
- текущая schema version: `3`.

Логи:

- GUI и служба ведут раздельные журналы;
- обычные события и ошибки пишутся всегда;
- подробные probe/runtime-события пишутся только при включённой галочке;
- максимум пять файлов на область;
- максимум 2 MiB на файл;
- общий предел примерно 10 MiB для GUI и 10 MiB для службы;
- отдельная запись ограничивается и очищается от управляющих переносов.

## 7. Проверка обновлений

GUI обращается к публичному GitHub Releases API:

- один раз после открытия окна;
- затем раз в 24 часа, пока приложение остаётся запущенным или находится в
  трее.

При более новой числовой версии:

- справа от основной кнопки появляется жёлтая иконка загрузки;
- подсказка содержит номер и краткое очищенное описание;
- клик открывает HTTPS-страницу релиза на `github.com`;
- автоматической загрузки или установки нет;
- tray-напоминание повторяется не чаще раза в три дня.

Сбой GitHub не влияет на runtime и попадает только в подробный лог.

## 8. Installer

Подготовлены:

- `installer/KROT.iss`;
- `installer/install-service.ps1`;
- `installer/uninstall-service.ps1`;
- Debug-скрипты установки и удаления службы.

Текущий Inno Setup installer ещё не считается проверенным пользовательским
артефактом. Не выполнены:

- сборка финального setup;
- установка на чистую Windows 10;
- установка на чистую Windows 11;
- проверка обновления поверх предыдущей версии;
- проверка удаления;
- подпись executable и installer;
- публикация релиза `1.0`.

## 9. Выполненный Debug-аудит 29 июля 2026

Статические проверки:

- PowerShell-скрипты установки/удаления разобраны parser без ошибок;
- `runtime-manifest.json` корректный JSON;
- поиск типовых секретов не обнаружил ключей и паролей;
- `git diff --check` проходит;
- `dotnet format --verify-no-changes` проходит;
- политика окончаний строк унифицирована на LF;
- NuGet audit не обнаружил известных уязвимых пакетов.

Сборка:

- `Debug x64`, `TreatWarningsAsErrors=true`;
- 0 предупреждений;
- 0 ошибок.

Тесты:

- всего 65;
- Core: 15;
- Infrastructure: 13;
- Zapret: 21;
- Diagnostics: 7;
- Service: 9;
- все тесты проходят.

Найден и исправлен дефект тестовой изоляции: service-тесты использовали
production-имя Named Pipe и могли зависнуть, если KROT был открыт. Теперь каждый
тестовый сервер использует уникальное имя pipe; полный набор проходит при
одновременно запущенном приложении.

UI smoke:

- Debug GUI запущен;
- доступность главных контролов подтверждена;
- окно «О приложении» открыто;
- версия `1.0` подтверждена;
- прокрутки в окне «О приложении» нет;
- снимок прозрачного WPF-окна через Windows Graphics Capture не поддерживается,
  поэтому проверка выполнена через UI Automation tree.

Lifecycle smoke:

- свежий Debug-сервис установлен из текущего output;
- SHA-256 установленного `KROT.Service.exe` совпал с исходным Debug-бинарником;
- тип запуска службы `Manual`;
- после закрытия GUI:
  - процессов `KROT`, `KROT.Service`, `winws`, `winws2` нет;
  - служба `KROTZapret` остановлена;
  - активных WinDivert-драйверов от KROT нет.

## 10. Что не подтверждено этим аудитом

VPN во время работы Codex оставался включён. Поэтому в этом checkpoint не
перепроверялись реальные результаты обхода Discord и YouTube без VPN.

Ранее рабочие стратегии и голосовой UDP-поток были проверены вручную, но перед
публичным релизом необходимо повторить на нескольких провайдерах:

1. Discord text/media.
2. Discord Voice с входом и выходом из канала.
3. YouTube site/video/QUIC.
4. Смена Wi-Fi.
5. Полный обрыв и восстановление сети.
6. Включение и выключение VPN во время активного KROT.

Жёлтая иконка обновления проверена тестами и XAML-сборкой, но не могла появиться
в живом GUI: в GitHub пока нет обычного числового релиза новее `1.0`.

## 11. Следующий этап

Следующая работа должна быть посвящена только установщику:

1. Определить точный состав файлов Release.
2. Проверить установку службы и ACL из installer.
3. Собрать локальный `KROT-Setup`.
4. Проверить install / update / uninstall.
5. Проверить отсутствие оставшихся процессов, службы, драйвера и файлов.
6. После успешного теста решить вопрос подписи и публикации релиза `1.0`.

До этого не создавать GitHub Release и не выдавать архив исходников за готовую
пользовательскую сборку.

## 12. Основные команды разработки

Строгая Debug-сборка:

```powershell
dotnet build KROT.sln -c Debug -p:Platform=x64 -p:TreatWarningsAsErrors=true
```

Тесты:

```powershell
dotnet test KROT.sln -c Debug -p:Platform=x64 --no-build
```

Проверка форматирования:

```powershell
dotnet format KROT.sln --verify-no-changes --no-restore
```

Установка свежей Debug-службы без запуска:

```powershell
.\scripts\install-debug-service.ps1 -NoStart
```

Запуск GUI:

```text
src\KROT.App\bin\x64\Debug\net48\KROT.exe
```

## 13. Установщик и release workflow 29 июля 2026

Подготовлен установщик `KROT-Setup-1.0-x64.exe` на Inno Setup 6.7.3:

- точное имя продукта `KROT zapret`;
- Windows 10/11 x64;
- проверка наличия .NET Framework 4.8;
- UAC требуется только для установки, обновления и удаления;
- приложение устанавливается в `C:\Program Files\KROT zapret`;
- в меню «Пуск» создаются ярлыки `KROT zapret` и `Удалить KROT zapret`;
- ярлык на рабочем столе остаётся необязательным;
- служба `KROTZapret` устанавливается с ручным типом запуска;
- GUI запускается без повышения прав и может запускать/останавливать службу через ограниченный ACL;
- PDB и XML-файлы разработчика в пакет не входят;
- runtime, WinDivert, Cygwin, лицензии и уведомления о сторонних компонентах входят в пакет;
- автозапуск не дублируется установщиком, им управляет настройка внутри KROT.

Добавлен `scripts/build-installer.ps1`. Скрипт проверяет совпадение версии,
выполняет строгую Release x64 сборку, все тесты, NuGet audit, проверяет
`runtime-manifest.json`, собирает установщик и создаёт SHA-256.

Добавлен ручной GitHub Actions workflow `release`:

- входные параметры: номер версии, краткое описание, режим `candidate` или `draft`;
- разрешены только числовые версии;
- сборка запускается только с `main`;
- Inno Setup 6.7.3 загружается с официального репозитория и проверяется по SHA-256;
- installer и checksum сохраняются как Actions artifact;
- создаётся provenance attestation;
- режим `draft` создаёт черновик GitHub Release, но не публикует его;
- публикация черновика остаётся ручным действием после проверки установщика.

Локальная проверка Release:

- сборка: 0 предупреждений, 0 ошибок;
- тесты: 65 из 65;
- известных уязвимых NuGet-пакетов нет;
- Inno Setup compilation: успешно;
- установка: успешно;
- повторная установка поверх 1.0: успешно;
- служба стартует и останавливается;
- найден и исправлен дефект ACL дочерних файлов службы;
- обычное закрытие GUI останавливает службу и выгружает KROT, KROT.Service,
  winws и winws2;
- удаление убирает папку приложения, службу и ярлыки;
- пользовательские настройки и логи при `/KEEPUSERDATA` сохранены без изменений;
- после теста восстановлена Debug-служба: Manual, Stopped.

Локальный проверенный installer до GitHub Actions имеет SHA-256:
`2dd5386e220cd69f1ada88b8abe256395b0cdf62d2483a459fd0d6e44d095bde`.
Финальный GitHub artifact будет иметь другой воспроизводимо рассчитанный hash,
поскольку в бинарники входит commit текущей сборки.

Удалённая публикация:

- commit: `16c45773751e04b449c794261b54125469a949cb`;
- push workflow `build`: успешно;
- release workflow run `30456703652`: успешно;
- создан черновик GitHub Release `KROT zapret 1.0`;
- tag release: `1.0`;
- target commit: `16c45773751e04b449c794261b54125469a949cb`;
- assets: installer и SHA-256;
- SHA-256 GitHub installer:
  `646372b2555667c9b4524daebdc5ab8658f5dfefc810ad9d3b48007fe9c573c3`;
- содержимое checksum-файла совпало с digest GitHub asset;
- релиз остаётся черновиком и не виден проверке обновлений до ручной публикации.

Текущее состояние машины после завершения удалённой сборки:

- установлен `KROT zapret 1.0` из commit `16c4577`;
- GUI `KROT.exe` открыт;
- служба `KROTZapret` запущена из `C:\Program Files\KROT zapret`;
- процессов `winws`/`winws2` нет;
- это активная пользовательская сессия KROT, поэтому она оставлена без изменений.

Релиз `1.0` опубликован 29 июля 2026 и назначен последним стабильным релизом:
https://github.com/morda-mir/KROT-zapret/releases/tag/1.0

## 14. Пересборка установщика 1.0 от 30 июля 2026

По результатам проверки антивирусных эвристик установщик 1.0 пересобран без
изменения номера версии:

- `Compression=zip`, `SolidCompression=no`;
- отключены Inno Setup Restart Manager и автоматическое закрытие приложений;
- PowerShell и `ExecutionPolicy Bypass` удалены из установки и удаления;
- регистрация, остановка и удаление службы выполняются `KROT.Service.exe`
  напрямую через Windows Service Control Manager;
- служба остаётся `Manual`, работает от `LocalSystem` и после установки
  находится в состоянии `Stopped`;
- неиспользуемый резервный runtime zapret2 1.0.2 исключён из пакета;
- рабочие zapret2 1.0.3 и zapret1 v72.13 сохранены;
- 34 стратегии сохранены: 16 TCP, 8 QUIC и 10 Discord Voice;
- при обновлении удаляются старый zapret2 1.0.2 и каталог PowerShell-скриптов;
- релизная сборка теперь всегда начинает с `dotnet clean`.

Проверки:

- строгая Release x64 сборка: успешно;
- все 65 тестов: успешно;
- NuGet vulnerability audit: уязвимостей нет;
- обновление поверх работающей службы: успешно;
- запуск и остановка службы: успешно;
- полное удаление с `/KEEPUSERDATA`: успешно;
- чистая повторная установка: успешно;
- установка точного CI-артефакта: успешно;
- удалённый EXE скачан обратно, его hash совпал с checksum.

Удалённая публикация:

- commit и tag `1.0`: `311969a652b5e76bd16502e5aeb34e94c1c47c97`;
- release workflow run: `30468750521`, успешно;
- версия и описание релиза не изменялись;
- размер installer: `5625168` байт;
- SHA-256:
  `f97e6a1383b2199dcc78c097039b855ef46e67afa8441a23a0098edc4e2cf4ee`;
- релиз:
  https://github.com/morda-mir/KROT-zapret/releases/tag/1.0

Текущее состояние машины:

- установлен точный CI-артефакт KROT zapret 1.0;
- служба `KROTZapret`: `Manual`, `Stopped`, `LocalSystem`;
- старого zapret2 1.0.2 и PowerShell-инструментов в Program Files нет.

## 15. Исправление проверки доступности от 30–31 июля 2026

По журналу пользователя обнаружено, что YouTube мог получать ложный зелёный
статус: проверка считала сервис доступным после ответа только от
`i.ytimg.com`, хотя сайт и видеопоток не работали.

Исправления:

- ответ домена изображений больше не считается подтверждением работы YouTube;
- для YouTube обязательны сайт, API и узел видеодоставки;
- для Discord обязательны gateway и media; API остаётся диагностической пробой,
  потому что его единичный тайм-аут не должен браковать фактически рабочую
  стратегию;
- подробный журнал различает DNS, TCP connect, TLS, reset и timeout;
- добавлены три резервные TCP/TLS-стратегии;
- общий каталог теперь содержит 37 вариантов: 19 TCP, 8 QUIC и 10 Discord
  Voice.

Проверки:

- строгая Release x64 сборка: успешно, предупреждений и ошибок нет;
- все 72 теста: успешно;
- release workflow run `30543551821`: успешно;
- build provenance attestation: успешно;
- установщик скачан обратно из опубликованного релиза;
- SHA-256 EXE совпал с опубликованным checksum.

Удалённая публикация:

- commit и tag `1.0`:
  `63411dbe32b820e260efcde5770dd760e097a9f9`;
- размер installer: `5626315` байт;
- SHA-256:
  `094de1c7b301121d72d82c19b7853c94c59f550c38f582b380278d96de0dc204`;
- версия и текст описания релиза не изменялись;
- релиз:
  https://github.com/morda-mir/KROT-zapret/releases/tag/1.0
